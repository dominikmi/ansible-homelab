---
- hosts: all
  tasks:
    - name: Install snapd
      dnf:
        name: snapd
        state: present
        update_cache: true
      become: true

    - name: Start snapd service
      ansible.builtin.systemd:
        name: snapd
        state: started
        enabled: true
      become: true
